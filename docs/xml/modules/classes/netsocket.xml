<?xml version="1.0"?>
<?xml-stylesheet type="text/xsl" href="class.xsl"?>

<book>
  <info>
    <name>NetSocket</name>
    <type>class</type>
    <module>Network</module>
    <comment>Manages network connections via TCP/IP sockets.</comment>
    <version>1</version>
    <id>b58a423b</id>
    <idstring>NETSOCKET</idstring>
    <category>Network</category>
    <copyright>Paul Manias © 2005-2026</copyright>
    <description>
<p>The NetSocket class provides a simple way of managing outbound TCP/IP socket communications.  It can connect to remote TCP servers, exchange UDP datagrams when the <code>UDP</code> flag is set, and optionally use SSL.  Inbound listening and accepted client management are provided by the <class name="NetServer">NetServer</class> subclass, with each accepted TCP connection represented by a <class name="ClientSocket">ClientSocket</class>.</p>
<p>The design of the NetSocket class caters to asynchronous (non-blocking) communication.  This is achieved primarily through callback fields - connection alerts are managed by <fl>Feedback</fl>, incoming data is received through <fl>Incoming</fl> and readiness for outgoing data is supported by <fl>Outgoing</fl>.</p>
<header>Outbound Connections</header>
<p>After a connection has been established, data may be written using any of the following methods:</p>
<list type="bullet">
<li>Write directly to the socket with the <action>Write</action> action.</li>
<li>Subscribe to the socket by referring to a routine in the <fl>Outgoing</fl> field.  The routine will be called to
initially fill the internal write buffer, thereafter it will be called whenever the buffer is empty.</li>
</list>
<p>It is possible to write to a NetSocket object before the connection to a server is established.  Doing so will buffer the data in the socket until the connection with the server has been initiated, at which point the data will be immediately sent.</p>
<header>Inbound Listeners</header>
<p>Use <class name="NetServer">NetServer</class> when a program needs to bind a local port and accept inbound clients.  NetServer inherits common socket fields from NetSocket, reports client state changes through <fl>Feedback</fl> with a <class name="ClientSocket">ClientSocket</class> parameter, and exposes active clients through its <code>Clients</code> field.  Data for accepted TCP clients is read from and written to <class name="ClientSocket">ClientSocket</class> objects.</p></description>
    <source>
      <file path="netsocket/">netsocket.cpp</file>
      <file path="netsocket/">netsocket_fields.cpp</file>
    </source>
  </info>

  <actions>
    <action>
      <name>DataFeed</name>
      <comment>Streams raw data to the socket for writing.</comment>
      <prototype>ERR acDataFeed(*Object, OBJECTID Object, DATA Datatype, APTR Buffer, INT Size)</prototype>
      <tiriPrototype>err = Object.acDataFeed(Object:obj, Datatype:DATA, Buffer:str)</tiriPrototype>
      <input>
        <param type="OBJECTID" name="Object">Must refer to the unique ID of the object that you represent. If you do not represent an object, set this parameter to the current task ID.</param>
        <param type="DATA" name="Datatype" lookup="DATA">The type of data being sent.</param>
        <param type="APTR" name="Buffer">The data being sent to the target object.</param>
        <param type="INT" name="Size">The size of the data in Buffer.</param>
      </input>
      <description>
<p>Data sent to a NetSocket through this action is written using the same buffered behaviour as <action>Write</action>.  If the feed size is zero, the buffer is treated as null-terminated text.</p>
      </description>
    </action>

    <action>
      <name>Disable</name>
      <comment>Disables sending and receiving on the socket.</comment>
      <prototype>ERR acDisable(*Object)</prototype>
      <tiriPrototype>err = Object.acDisable()</tiriPrototype>
      <description>
<p>This method will stop all sending and receiving of data over the socket.  This is irreversible.</p>
      </description>
      <result>
        <error code="Okay">Operation successful.</error>
        <error code="Failed">Shutdown operation failed.</error>
      </result>
    </action>

    <action>
      <name>Read</name>
      <comment>Read information from the socket.</comment>
      <prototype>ERR acRead(*Object, APTR Buffer, INT Length, INT *Result)</prototype>
      <tiriPrototype>err, Result:num = Object.acRead(Buffer:str, Length:num)</tiriPrototype>
      <input>
        <param type="APTR" name="Buffer">Points a buffer that will receive the data.</param>
        <param type="INT" name="Length">The total number of bytes to read from the object.  This value cannot exceed the size of the Buffer.</param>
        <param type="INT" name="Result">The Read action will write this parameter with the total number of bytes read into the Buffer.</param>
      </input>
      <description>
<p>The Read() action will read incoming data from the socket and write it to the provided buffer.  If the socket connection is safe, success will always be returned by this action regardless of whether or not data was available.  Almost all other return codes indicate permanent failure and the socket connection will be closed when the action returns.</p>
<p>Because NetSocket objects are non-blocking, reading from the socket is normally performed in the <fl>Incoming</fl> callback.  Reading from the socket when no data is available will result in an immediate return with no output.</p>
      </description>
      <result>
        <error code="Okay">Read successful (if no data was on the socket, success is still indicated).</error>
        <error code="Failed">A permanent failure has occurred and socket has been closed.</error>
        <error code="InvalidState">The socket is not in a state that allows reading (e.g. during SSL handshake).</error>
        <error code="Disconnected">The socket connection is closed.</error>
        <error code="NullArgs">Function call missing argument value(s)</error>
      </result>
    </action>

    <action>
      <name>Write</name>
      <comment>Writes data to the socket.</comment>
      <prototype>ERR acWrite(*Object, APTR Buffer, INT Length, INT Result)</prototype>
      <tiriPrototype>err, Result:num = Object.acWrite(Buffer:str, Length:num)</tiriPrototype>
      <input>
        <param type="APTR" name="Buffer">A buffer containing the data that will be written to the object.</param>
        <param type="INT" name="Length">The total number of bytes to write to the object.</param>
        <param type="INT" name="Result">This parameter with be updated with the total number of bytes written from the Buffer.</param>
      </input>
      <description>
<p>Writing data to a socket will send raw data to the remote client or server.  Write connections are buffered, so any data overflow generated in a call to this action will be buffered into a software queue.  Resource limits placed on the software queue are governed by the <fl>MsgLimit</fl> field setting.</p>
<p>Do not use this action on a <class name="NetServer">NetServer</class> listener.  Instead, write to the <class name="ClientSocket">ClientSocket</class> object that will receive the data.</p>
<p>It is possible to write to a socket in advance of any connection being made. The netsocket will queue the data and automatically send it once the first connection has been made.</p>
      </description>
    </action>

  </actions>

  <methods>
    <method>
      <name>Connect</name>
      <comment>Connects a NetSocket to an address.</comment>
      <prototype>ERR ns::Connect(OBJECTPTR Object, CSTRING Address, INT Port, DOUBLE Timeout)</prototype>
      <tiriPrototype>err = Connect(Address:str, Port:num, Timeout:num)</tiriPrototype>
      <input>
        <param type="CSTRING" name="Address">String containing either a domain name (e.g. <code>www.google.com</code>) or an IP address (e.g. <code>123.123.123.123</code>)</param>
        <param type="INT" name="Port">Remote port to connect to.</param>
        <param type="DOUBLE" name="Timeout">Connection timeout in seconds (0 = no timeout).</param>
      </input>
      <description>
<p>This method initiates the connection process with a target IP address.  The address to connect to can be specified either as a domain name, in which case the domain name is first resolved to an IP address, or the address can be specified in standard IP notation.</p>
<p>This method is non-blocking.  It will return immediately and the connection will be resolved once the server responds to the connection request or an error occurs.  Client code should subscribe to the <fl>State</fl> field to respond to changes to the connection state.</p>
<p>Pre-Condition: Must be in a connection state of <code>NTC::DISCONNECTED</code></p>
<p>Post-Condition: If this method returns <code>ERR::Okay</code>, will be in state <code>NTC::CONNECTING</code>.</p>
      </description>
      <result>
        <error code="Okay">The NetSocket connecting process was successfully started.</error>
        <error code="Failed">The connect failed for some other reason.</error>
        <error code="Args">Address was NULL, or Port was not in the required range.</error>
        <error code="TimeOut">Connection attempt timed out.</error>
        <error code="InvalidState">The NetSocket was not in the state <code>NTC::DISCONNECTED</code> or the object is a <class name="NetServer">NetServer</class>.</error>
        <error code="HostNotFound">Host name resolution failed.</error>
      </result>
      <tags>non-blocking, mutates-object, copies-input</tags>
    </method>

    <method>
      <name>GetLocalIPAddress</name>
      <comment>Returns the IP address that the socket is locally bound to.</comment>
      <prototype>ERR ns::GetLocalIPAddress(OBJECTPTR Object, struct IPAddress * Address)</prototype>
      <tiriPrototype>err = GetLocalIPAddress(Address:any)</tiriPrototype>
      <input>
        <param type="struct IPAddress *" name="Address">Pointer to an IPAddress structure which will be set to the result of the query if successful.</param>
      </input>
      <description>
<p>This method performs the POSIX equivalent of <code>getsockname()</code>.  It returns the current address to which the NetSocket is bound.</p>
      </description>
      <result>
        <error code="Okay">Operation successful.</error>
        <error code="Failed">General failure</error>
        <error code="NullArgs">Function call missing argument value(s)</error>
      </result>
      <tags>pure-query, mutates-input</tags>
    </method>

    <method>
      <name>JoinMulticastGroup</name>
      <comment>Join a multicast group for receiving multicast packets (UDP only).</comment>
      <prototype>ERR ns::JoinMulticastGroup(OBJECTPTR Object, CSTRING Group)</prototype>
      <tiriPrototype>err = JoinMulticastGroup(Group:str)</tiriPrototype>
      <input>
        <param type="CSTRING" name="Group">The multicast group address to join (e.g. <code>224.1.1.1</code>).</param>
      </input>
      <description>
<p>This method joins a multicast group, allowing the socket to receive packets sent to the specified multicast address. This is only available for UDP sockets.</p>
<p>The socket must be bound to a local address before joining a multicast group.</p>
      </description>
      <result>
        <error code="Okay">Successfully joined the multicast group.</error>
        <error code="Failed">Failed to join multicast group.</error>
        <error code="Args">Invalid multicast address.</error>
        <error code="NoSupport">Socket is not configured for UDP mode.</error>
      </result>
      <tags>mutates-object</tags>
    </method>

    <method>
      <name>LeaveMulticastGroup</name>
      <comment>Leave a multicast group (UDP only).</comment>
      <prototype>ERR ns::LeaveMulticastGroup(OBJECTPTR Object, CSTRING Group)</prototype>
      <tiriPrototype>err = LeaveMulticastGroup(Group:str)</tiriPrototype>
      <input>
        <param type="CSTRING" name="Group">The multicast group address to leave.</param>
      </input>
      <description>
<p>This method leaves a previously joined multicast group, stopping the reception of packets sent to the specified multicast address.</p>
      </description>
      <result>
        <error code="Okay">Successfully left the multicast group.</error>
        <error code="Failed">Failed to leave multicast group.</error>
        <error code="Args">Invalid multicast address.</error>
        <error code="NoSupport">Socket is not configured for UDP mode.</error>
      </result>
      <tags>mutates-object</tags>
    </method>

    <method>
      <name>RecvFrom</name>
      <comment>Receive a datagram packet from any address (UDP only).</comment>
      <prototype>ERR ns::RecvFrom(OBJECTPTR Object, struct IPAddress * Source, APTR Buffer, INT BufferSize, INT * BytesRead)</prototype>
      <tiriPrototype>err, BytesRead:num = RecvFrom(Source:any, Buffer:any, BufferSize:num)</tiriPrototype>
      <input>
        <param type="struct IPAddress *" name="Source">Source IP address of the received packet.</param>
        <param type="APTR" name="Buffer">Output buffer for received data.</param>
        <param type="INT" name="BufferSize">Size of the receive buffer in bytes.</param>
        <param type="INT *" name="BytesRead">Number of bytes actually received.</param>
      </input>
      <description>
<p>This method receives a datagram packet from any source address.  It is only available for sockets configured with the UDP flag.  Unlike TCP connections, UDP is connectionless so packets can be received from any source without establishing a connection first.</p>
<p>The method is non-blocking and will return immediately.  If no data is available, <code>ERR::Okay</code> will be returned with <code>BytesRead</code> set to zero.</p>
<p>The source address and port of the received packet will be provided in the output parameters.</p>
<p>For TCP sockets, use the standard Read action instead.</p>
      </description>
      <result>
        <error code="Okay">Data was received successfully, or no data available.</error>
        <error code="Args">Invalid arguments provided.</error>
        <error code="NoSupport">Socket is not configured for UDP mode.</error>
        <error code="BufferOverflow">Receive buffer is too small for the incoming packet.</error>
      </result>
      <tags>non-blocking, mutates-input, mutates-object</tags>
    </method>

    <method>
      <name>SendTo</name>
      <comment>Send a datagram packet to a specific address (UDP only).</comment>
      <prototype>ERR ns::SendTo(OBJECTPTR Object, struct IPAddress * Dest, APTR Data, INT Length, INT * BytesSent)</prototype>
      <tiriPrototype>err, BytesSent:num = SendTo(Dest:any, Data:any, Length:num)</tiriPrototype>
      <input>
        <param type="struct IPAddress *" name="Dest">The destination IP address (IPv4 or IPv6) and port number.</param>
        <param type="APTR" name="Data">Pointer to the data buffer to send.</param>
        <param type="INT" name="Length">Number of bytes to send from Data.</param>
        <param type="INT *" name="BytesSent">Number of bytes actually sent.</param>
      </input>
      <description>
<p>This method sends a datagram packet to a specified IP address and port.  It is only available for sockets configured with the UDP flag.  Unlike TCP connections, UDP is connectionless so packets can be sent to any address without establishing a connection first.</p>
<p>The method is non-blocking and will return immediately.  If the network buffer is full, an <code>ERR::BufferOverflow</code> error will be returned and the client should retry the operation later.</p>
<p>For TCP sockets, use the standard Write action instead.</p>
      </description>
      <result>
        <error code="Okay">The packet was sent successfully.</error>
        <error code="OutOfRange">Invalid port number specified.</error>
        <error code="BufferOverflow">The network buffer is full, retry later.</error>
        <error code="InvalidState">Socket is not configured for UDP mode.</error>
        <error code="NetworkUnreachable">The destination network is unreachable.</error>
        <error code="NullArgs">Invalid arguments provided.</error>
      </result>
      <tags>non-blocking, consumes-input</tags>
    </method>

  </methods>

  <fields>
    <field>
      <name>Address</name>
      <comment>An IP address or domain name to connect to.</comment>
      <access read="R" write="I">Read/Init</access>
      <type>std::string</type>
      <description>
<p>For NetSocket clients, if this field is set with an IP address or domain name prior to initialisation, an attempt to connect to that location will be made when the object is initialised.  Post-initialisation this field cannot be set by the client, however calls to <method>Connect</method> will result in it being updated so that it always reflects the named address of the current connection.</p>
<p>For <class name="NetServer">NetServer</class> listeners, this inherited field identifies the local address to bind before initialisation.  Use <code>localhost</code>, <code>*</code>, an IPv4 address or an IPv6 address.</p>
      </description>
    </field>

    <field>
      <name>ClientData</name>
      <comment>A client-defined value that can be useful in action notify events.</comment>
      <access read="R" write="W">Read/Write</access>
      <type>APTR</type>
      <description>
<p>This is a free-entry field value that can store client data for future reference.</p>
      </description>
    </field>

    <field>
      <name>Error</name>
      <comment>Information about the last error that occurred during a NetSocket operation</comment>
      <access read="R">Read</access>
      <type>ERR</type>
      <description>
<p>This field describes the last error that occurred during a NetSocket operation:</p>
<p>In the case where a NetSocket object enters the <code>NTC::DISCONNECTED</code> state from the <code>NTC::CONNECTED</code> state, this field can be used to determine how a TCP connection was closed.</p>
<types type="Error">
<type name="ERR::Okay">The connection was closed gracefully.  All data sent by the peer has been received.</type>
<type name="ERR::Disconnected">The connection was broken in a non-graceful fashion. Data may be lost.</type>
<type name="ERR::TimeOut">The connect operation timed out.</type>
<type name="ERR::ConnectionRefused">The connection was refused by the remote host.  Note: This error will not occur on Windows, and instead the Error field will be set to <code>ERR::Failed</code>.</type>
<type name="ERR::NetworkUnreachable">The network was unreachable.  Note: This error will not occur on Windows, and instead the Error field will be set to <code>ERR::Failed</code>.</type>
<type name="ERR::HostUnreachable">No path to host was found.  Note: This error will not occur on Windows, and instead the Error field will be set to <code>ERR::Failed</code>.</type>
<type name="ERR::Failed">An unspecified error occurred.</type>
</types>
      </description>
    </field>

    <field>
      <name>Feedback</name>
      <comment>A callback trigger for when the state of the NetSocket is changed.</comment>
      <access read="G" write="S">Get/Set</access>
      <type>FUNCTION</type>
      <description>
<p>The client can define a function in this field to receive notifications whenever the state of the socket changes - typically connection messages.</p>
<p>For NetSocket clients, the function must follow the prototype <code>Function(*NetSocket, NTC State)</code>.  For <class name="NetServer">NetServer</class> listeners, the inherited field uses <code>Function(*NetServer, *ClientSocket, NTC State)</code>.</p>
<p>The first parameter refers to the object to which the function is subscribed.  For NetServer listeners, <code>ClientSocket</code> refers to the <class name="ClientSocket">ClientSocket</class> on which the state has changed.</p>
      </description>
    </field>

    <field>
      <name>Flags</name>
      <comment>Optional flags.</comment>
      <access read="R" write="W">Read/Write</access>
      <type lookup="NSF">NSF</type>
      <description>
<types lookup="NSF"/>
      </description>
    </field>

    <field>
      <name>Handle</name>
      <comment>Platform specific reference to the network socket handle.</comment>
      <access read="G" write="S">Get/Set</access>
      <type>APTR</type>
    </field>

    <field>
      <name>Incoming</name>
      <comment>Callback that is triggered when the socket receives data.</comment>
      <access read="G" write="S">Get/Set</access>
      <type>FUNCTION</type>
      <description>
<p>The Incoming field can be set with a custom function that will be called whenever the socket receives data.  The function prototype for C++ is <code>ERR Incoming(*NetSocket, APTR Meta)</code>.  For Tiri use <code>function Incoming(NetSocket)</code>.</p>
<p>The <code>NetSocket</code> parameter refers to the NetSocket object.  <code>Meta</code> is optional userdata from the <code>FUNCTION</code>.  For <class name="NetServer">NetServer</class> listeners, this inherited field receives <code>Incoming(*NetServer, *ClientSocket, APTR Meta)</code> in C++ or <code>function Incoming(NetServer, ClientSocket)</code> in Tiri, and data must be read from the supplied <class name="ClientSocket">ClientSocket</class>.</p>
<p>Retrieve data from the socket with the <action>Read</action> action. Reading at least some of the data from the socket is compulsory - if the function does not do this then the data will be cleared from the socket when the function returns. If the callback function returns/raises <code>ERR::Terminate</code> then the Incoming field will be cleared and the function will no longer be called.  All other error codes are ignored.</p>
      </description>
    </field>

    <field>
      <name>MaxPacketSize</name>
      <comment>Maximum UDP packet size for sending and receiving data.</comment>
      <access read="R" write="I">Read/Init</access>
      <type>INT</type>
      <description>
<p>This field sets the maximum size in bytes for UDP packets when sending or receiving data.  It only applies to UDP sockets and is ignored for TCP connections.  The default value is 65507 bytes, which is the maximum payload size for UDP packets (65535 - 8 bytes UDP header - 20 bytes IP header).</p>
<p>If you attempt to send a packet larger than MaxPacketSize, a warning will be logged and the operation may fail. When receiving data, packets larger than this size will be truncated.</p>
      </description>
    </field>

    <field>
      <name>MsgLimit</name>
      <comment>Limits the size of incoming and outgoing data packets.</comment>
      <access read="R" write="I">Read/Init</access>
      <type>INT</type>
      <description>
<p>This field limits the size of incoming and outgoing message queues (each socket connection receives two queues assigned to both incoming and outgoing messages).  The size is defined in bytes.  Sending or receiving messages that overflow the queue results in the connection being terminated with an error.</p>
<p>The default setting is 1 megabyte.</p>
      </description>
    </field>

    <field>
      <name>MulticastTTL</name>
      <comment>Time-to-live (hop limit) for multicast packets.</comment>
      <access read="R" write="I">Read/Init</access>
      <type>INT</type>
      <description>
<p>This field sets the time-to-live (TTL) value for multicast packets sent from UDP sockets.  The TTL determines how many network hops (routers) a multicast packet can traverse before being discarded.  This helps prevent multicast traffic from flooding the network indefinitely.</p>
<p>The default TTL is 1, which restricts multicast to the local network segment.  Higher values allow multicast packets to traverse more network boundaries:</p>
<list type="bullet">
<li>1: Local network segment only</li>
<li>32: Within the local site</li>
<li>64: Within the local region</li>
<li>128: Within the local continent</li>
<li>255: Unrestricted (global)</li>
</list>
      </description>
    </field>

    <field>
      <name>OutQueueSize</name>
      <comment>The number of bytes on the socket's outgoing queue.</comment>
      <access read="G">Get</access>
      <type>INT</type>
    </field>

    <field>
      <name>Outgoing</name>
      <comment>Callback that is triggered when a socket is ready to send data.</comment>
      <access read="G" write="S">Get/Set</access>
      <type>FUNCTION</type>
      <description>
<p>The Outgoing field can be set with a custom function that will be called whenever the socket is ready to send data. For NetSocket clients the function must be in the format <code>ERR Outgoing(*NetSocket, APTR Meta)</code>.  For <class name="NetServer">NetServer</class> listeners, the inherited field uses <code>ERR Outgoing(*NetServer, *ClientSocket, APTR Meta)</code>.</p>
<p>To send data from a NetSocket object, call the <action>Write</action> action.  To send data from a NetServer to a connected client, call <class name="ClientSocket" method="Write">ClientSocket.Write()</class> on the target client socket.  If the callback function returns an error other than <code>ERR::Okay</code> then the Outgoing field will be cleared and the function will no longer be called.</p>
      </description>
    </field>

    <field>
      <name>Port</name>
      <comment>The port number to use for connections.</comment>
      <access read="R" write="I">Read/Init</access>
      <type>INT</type>
      <description>
<p>For NetSocket clients, this is the remote port used by <method>Connect</method>.  For <class name="NetServer">NetServer</class> listeners, this inherited field is the local port to bind during initialisation.</p>
      </description>
    </field>

    <field>
      <name>State</name>
      <comment>The current connection state of the NetSocket object.</comment>
      <access read="G" write="S">Get/Set</access>
      <type lookup="NTC">NTC</type>
      <description>
<p>The State reflects the connection state of the NetSocket.  If the <fl>Feedback</fl> field is defined with a function, it will be called automatically whenever the state is changed.  Note that the ClientSocket parameter will be NULL when the Feedback function is called.</p>
<p>For <class name="NetServer">NetServer</class> listeners this State value should not be used as it cannot reflect the state of all connected client sockets.  When read from a NetServer, this inherited field reports <code>NTC::MULTISTATE</code>; each <class name="ClientSocket">ClientSocket</class> carries its own independent State value for accepted TCP connections.</p>
<types lookup="NTC"/>
      </description>
    </field>

  </fields>
  <types>
    <constants lookup="NSF" comment="NetSocket options">
      <const name="BROADCAST">Enable broadcast (UDP only).</const>
      <const name="DISABLE_SERVER_VERIFY">Disable SSL server certificate verification for client sockets (testing only).</const>
      <const name="KEEP_ALIVE">Enable TCP keep-alive probes using operating system defaults.</const>
      <const name="LOG_ALL">Print extra log messages.</const>
      <const name="MULTI_CONNECT">Allow multiple connections from the same IP (NetServer only).</const>
      <const name="SSL">Use Secure Sockets Layer for all communication.</const>
      <const name="SYNCHRONOUS">Use synchronous (blocking) network calls.</const>
      <const name="UDP">Use UDP (connectionless datagram protocol) instead of TCP.</const>
    </constants>

    <constants lookup="NTC" comment="NetSocket states">
      <const name="CONNECTED">There is an active connection at present.</const>
      <const name="CONNECTING">A connection is being established.</const>
      <const name="DISCONNECTED">There is no connection.</const>
      <const name="HANDSHAKING">An SSL connection is being established.</const>
      <const name="MULTISTATE">A NetServer reports MULTISTATE because accepted ClientSocket objects track their own states.</const>
      <const name="RESOLVING">The host name is being resolved.</const>
    </constants>

  </types>
  <structs>
    <struct name="IPAddress">
      <field name="Data" type="INT" size="4">128-bit array for supporting both V4 (32-bit host order) and V6 (8-bit byte order) IP addresses.</field>
      <field name="Type" type="IPADDR" lookup="IPADDR">Identifies the address Data value as a V4 or V6 address type.</field>
      <field name="Port" type="INT">For UDP packets, identifies the client port number in host byte order.</field>
    </struct>

  </structs>
</book>
